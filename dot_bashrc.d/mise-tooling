# Activate mise tool manager
if command -v mise &> /dev/null; then
    eval "$(mise activate bash)"
    eval "$(mise completion bash)"
fi

# Sheldon shell plugin management
if command -v sheldon &> /dev/null; then
    eval "$(sheldon source)"
fi
